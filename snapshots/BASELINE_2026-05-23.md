# Baseline snapshot (2026-05-23)

This repo snapshot exists so we can confidently try Option A and, if it's not good, revert to the exact same state and proceed with Option B.

## Git anchor

- Branch: `main`
- Commit SHA: `5037864600cac106619c368d770f3db442930d7f`
- Commit message: `Add Jira verification GitHub Action and ignore macOS files`

Revert options:

- Hard reset (local only): `git reset --hard 5037864600cac106619c368d770f3db442930d7f`
- Revert commit(s) (keeps history): `git revert <sha>` (for commits made during Option A)

## Safe A/B trial workflow

Rules:

1. Each attempt happens on its own branch (for example `option-a/*`).
2. Only merge if it looks good.
3. If not, delete the branch and `main` stays unchanged.

Repo contents at baseline are recorded in `FILETREE_2026-05-23.txt` and git cleanliness in `GIT_STATUS_2026-05-23.txt`.
